export const metadata = {
  title: 'Captura NFS-e',
  description: 'Painel de configuração para captura NFS-e'
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
